# `wrangler deploy` publishes this folder as static assets: nothing listed here is ever served.
# Keep in step with .gitignore (SHIP.md lists what ships). _headers must NOT be listed — wrangler reads it.
/src
wrangler.toml
.assetsignore
.gitignore
.git
.wrangler
.claude
.rgignore
node_modules/

# notes and docs
*.md

# build-time tooling and its inputs
tools/
*.mjs
*.csv
sql/

# the osrs-r2 transcode tree (~1.9GB): too big for Workers Assets; served from R2 bucket test1 at https://assets.vla.dev
out/
play-local.cmd

# retired: the temporary wiki sprite dumps and the old model bundle
assets/c07-full2/
assets/i07-full2/
assets/osrs/
